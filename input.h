/**
* Header file for Input program with line reading declaration
* @file input.h
* @author Pranav Kale (pkale)
*/

#include <stdio.h>

/**
* Declaration of the read line method in input 
* @param fp the file to read in 
* @return the command read in
*/
char *readLine(FILE *fp);